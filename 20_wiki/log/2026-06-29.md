# 2026-06-29 Log

## decision+update | 위키 연동 방식 절대경로 → 심볼릭링크 전환 (`0.7.1` → `0.8.0`, minor)

- 문제: 기존 연동은 소비 프로젝트 AGENTS.md가 셸로 홈 절대경로를 풀어 `$HOME/.llm-wiki`를 직접 여는 방식 → 절대경로 하드코딩·머신차 처리 부담, `$HOME`/`~`를 읽기 도구에 넘기는 실수 유발.
- 결정: 위키 실체는 `$HOME/.llm-wiki` 한 곳에 두고, 소비 프로젝트는 루트에 `.llm-wiki` **심볼릭링크**(포인터)를 만들어 상대경로 `./.llm-wiki/...`로 읽는다. 링크는 절대경로라 머신마다 깨지므로 소비 프로젝트 `.gitignore`에 `.llm-wiki` 선언 필수, 머신마다 부트스트랩으로 재생성.
- bump 근거: [[위키 버전 관리]] MINOR #3(작동 방식 구조 변경) — 에이전트의 위키 접근·경로 처리 절차가 실제로 달라짐.

### 변경 문서
- `AGENTS.md`(운영): `## 프로젝트 연동` 섹션 신설 — 실체 단일 위치·심볼릭링크 개념 + **git 대상 구분**(본진 repo는 정상 push / 소비 프로젝트 링크는 커밋 금지·`.gitignore` 필수). 상세는 [[AGENTS.md 작성 가이드]]를 SSOT로 위임.
- [[AGENTS.md 작성 가이드]]: 절대경로 방식 전면 폐기 → 심볼릭링크로 재작성. 핵심원칙 4번(심볼릭링크 연동), 공통 위키 위치/연동 방식, git 대상 구분표, 부트스트랩 절차(`ln -sf`·Windows `New-Item SymbolicLink`·gitignore·검증), 로컬/원격 분기(로컬 있으면 심볼릭링크 읽기, 없으면 원격 index.md 읽고 clone→링크), 읽기 규칙 상대경로화, 흔한 실수표 갱신.

### 정합(중복 제거)
- 본진 `AGENTS.md` 프로젝트 연동에서 부트스트랩 절차·소비 템플릿 복붙 제거 → [[AGENTS.md 작성 가이드]] 단일 기준 링크.
- [[AGENTS.md 작성 가이드]] 내부: "전체 템플릿"의 Wiki Baseline을 "삽입 전용 블록과 동일"로 축약(중복 ~10줄 제거).
- [[LLM Wiki와 옵시디언으로 프론트엔드 작업 자동화하기]]: "AGENTS.md에 git 경로 추가 참조"는 초기 설계임을 명시하는 현행화 노트 추가(스토리·다이어그램은 히스토리로 유지) + 가이드 링크.
- [[위키 개요]] 운영 루프에 "정확한 절차·SSOT는 `AGENTS.md` 작업 흐름, 충돌 시 우선" 콜아웃 추가(표류 방지).

## update | 위키 변경 워크플로 브랜치·PR 필수화 (`0.8.0` → `0.9.0`, minor)

- 결정: `main` 직접 커밋·push 금지. 위키 변경은 항상 작업 브랜치 → push → PR로만 반영. 버전 bump의 tag·Release는 PR 머지 후 생성.
- bump 근거: [[위키 버전 관리]] 경계 케이스 "AGENTS 흐름에 강제 단계 생김 → minor" — 에이전트 반영 절차가 직접 커밋에서 PR 필수로 바뀜.
- 변경 문서:
  - `AGENTS.md`(운영): `### Branch · PR (필수)` 신설, `Versioning` 절차를 PR 머지 후 tag·Release로 수정.
  - [[MR PR 작성 가이드]]: §9 "위키 PR 템플릿"(이 저장소 전용) 추가 — UI 없으므로 스크린샷·테스트 생략, 변경요약·변경문서·버전·체크 4블록 간단 템플릿.
  - [[위키 버전 관리]]: 작업 절차 7~9을 브랜치 커밋→PR→머지 후 tag로 갱신.
  - PR은 **반드시 squash merge**(`main` 이력 1커밋/PR). 세 문서(AGENTS·MR PR 가이드·위키 버전 관리)에 명시.

## update | LLM Wiki 기술결정 문서 발행본 기반 전면 재작성 (`0.9.0` → `0.9.1`, patch)

- [[LLM Wiki와 옵시디언으로 프론트엔드 작업 자동화하기]]를 LinkedIn 발행본("멀티 프로젝트의 관리 리소스를 줄이는 계층적 LLM-WIKI 방법론", 정회석) 기준으로 전면 재작성.
- 기존 초안 내러티브 폐기, 발행본 흐름 반영: 배경(신입 컨벤션 누락) → LLM-WIKI 정의·useEffect 맥락 케이스 → 멀티프로젝트 공통 Store 분리 → 개별 프로젝트 위키(raw/summaries/decisions/worklog) → AGENTS.md 진입점 참조 → 공통 경로 대안 4개 비교표(서브모듈·env·network fetch·clone✅) → 외부참조 해결 심볼릭링크 → Obsidian+Git → 적용 결과.
- frontmatter: `status` draft→verified, `source` user-notes→LinkedIn URL, `updated` 2026-06-29.
- 파일명·H1 유지(과거 append-only 로그 `2026-06-23`의 `[[링크]]` 보존). 출처에 LinkedIn·공개 저장소·Karpathy gist 명시.
- patch 근거: [[위키 버전 관리]] "판단 기준 조정 / 기존 문서 보강" — 단일 기술결정 문서 재작성, 위키 범위 불변.

## restructure | 루트 폴더 번호 프리픽스 + `.obsidian` 추적 해제 (`0.9.1` → `0.10.0`, minor)

- 루트 폴더에 정렬·파이프라인 순서용 번호 프리픽스: `context` → `00_context`, `raw` → `10_raw`, `wiki` → `20_wiki` (10단위 간격으로 삽입 여유).
- 진입경로 변경: 소비 프로젝트 읽기 경로 `./.llm-wiki/wiki/index.md` → `./.llm-wiki/20_wiki/index.md`. **심볼릭링크 이름 `.llm-wiki`는 그대로**(repo·링크명 불변, 내부 폴더만 rename).
- sweep 범위: `AGENTS.md`·`CLAUDE.md`·`20_wiki/` living 문서의 경로 리터럴(폴더역할·`source:`·읽기경로·운영절차) 일괄 치환. `10_raw/` 원본과 `20_wiki/log/` 과거 기록은 보존(미수정).
- `.gitignore`: `.obsidian/` 통째 제외로 변경(기존 workspace·cache 부분 제외 → 전체). 추적 중이던 `.obsidian/*`는 `git rm --cached`로 해제 — 플러그인·테마 등 머신별 설정을 git에서 분리.
- bump 근거: [[위키 버전 관리]] MINOR #3(폴더 역할 재정의·경로 구조 변경) — 에이전트 읽기 경로가 실제로 달라짐.
- 미반영(별도): 소비 프로젝트 `wiki-import-test/AGENTS.md`의 읽기경로도 `20_wiki`로 갱신 필요(다른 repo).

## fix | `.obsidian` 통째 ignore 되돌려 공유 설정 재추적 (`0.10.0` → `0.10.1`, patch)

- 직전 변경에서 `.obsidian/`를 통째 ignore·추적 해제했으나, 플러그인·테마 등 머신 간 공유 설정까지 끊김 → 부분 ignore로 복구.
- `.gitignore`를 `.obsidian/workspace.json`·`workspace-mobile.json`·`cache/`만 제외로 환원(machine-specific·volatile만 제외).
- 공유 설정 재추적: `app.json`·`appearance.json`·`community-plugins.json`·`core-plugins.json`·`graph.json`·`plugins/mermaid-tools/*` (제거 전 커밋 `2f7698f`에서 복원).
- patch 근거: [[위키 버전 관리]] "기존 컨벤션 보강 / 링크·설정 보정" — 운영 범위·동작 불변.

## lint | 깨진 위키링크 수정 ([[Hermes]]) (`0.10.1` → `0.10.2`, patch)

- 폴더 번호 작업 후 링크 전수 감사 → 옛 경로 토큰·진입경로·`source:` 전부 정상 확인(rename 누락 0).
- 발견: [[Hermes]]의 `[[New Architecture]]`가 깨짐 — `New Architecture`는 독립 노트가 아니라 [[React Native]] 내 `## New Architecture` 섹션. (rename과 무관, 기존부터 깨짐)
- 수정: `[[New Architecture]]` → `[[React Native#New Architecture]]` 섹션 링크.
- 참고: AGENTS.md의 `[[Wikilink]]`·`[[링크]]`는 예시 플레이스홀더로 정상(실제 링크 아님).
