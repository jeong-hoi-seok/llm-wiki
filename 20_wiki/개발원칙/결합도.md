---
type: concept
platform: common
status: verified
tags: [frontend, code-quality, coupling]
source: 10_raw/toss/frontend-fundamentals/code-quality/
created: 2026-06-19
updated: 2026-06-19
---

# 결합도

## 한 줄 요약
**코드를 수정했을 때의 영향 범위.** 영향 범위가 작아 변경 파급을 예측할 수 있는 코드가 수정하기 쉽다. [[코드 품질]] 4기준 중 하나.

## 전략
- **책임을 하나씩 관리** — 한 훅/함수가 여러 책임을 지면 한 곳 수정이 전부에 파급. (raw: `examples/use-page-state-coupling.md`)
- **중복 코드 허용** — 섣부른 공통화로 강결합 만드는 대신, 일부 중복을 허용해 영향 범위 격리. (raw: `examples/use-bottom-sheet.md`)
- **Props Drilling 지우기** — 깊은 prop 전달은 중간 컴포넌트를 강하게 결합. Composition·Context로 해소. (raw: `examples/item-edit-modal.md`)

## 빠른 참조

| 코드 냄새 | 개선 방법 |
|---|---|
| 3개 이상 계층 통과하는 props | 조합 패턴: children 사용 |
| 5개 이상 반환하는 Hook | 단일 책임 Hook으로 분리 |
| A 수정 시 관련 없는 B 깨짐 | 결합 지점 식별 후 인터페이스 도입 |
| 모든 곳에서 import하는 util | 사용처 가까이로 이동 |

## 코드 예제

### Props Drilling 제거 (조합 패턴)

❌ props가 계층을 통과:
```tsx
function ItemEditModal({ items, onConfirm, onClose }) {
  return (
    <Modal onClose={onClose}>
      <ItemEditBody items={items} onConfirm={onConfirm} onClose={onClose} />
    </Modal>
  );
}
function ItemEditBody({ items, onConfirm, onClose }) {
  return (
    <>
      <Button onClick={onClose}>닫기</Button>
      <ItemEditList items={items} onConfirm={onConfirm} />
    </>
  );
}
```

✅ children으로 조합:
```tsx
function ItemEditModal({ items, onConfirm, onClose }) {
  return (
    <Modal onClose={onClose}>
      <ItemEditBody onClose={onClose}>
        <ItemEditList items={items} onConfirm={onConfirm} />
      </ItemEditBody>
    </Modal>
  );
}
function ItemEditBody({ children, onClose }) {
  return <><Button onClick={onClose}>닫기</Button>{children}</>;
}
```

### 책임 분리

❌ 모든 것을 관리하는 Hook:
```tsx
function usePageState() {
  const [query] = useQueryParams({
    cardId: NumberParam, dateFrom: DateParam, dateTo: DateParam, statusList: ArrayParam
  });
}
```

✅ 단일 책임 Hook:
```tsx
function useCardIdQueryParam() {
  const [cardId, setCardId] = useQueryParam("cardId", NumberParam);
  return [cardId ?? undefined, setCardId] as const;
}
```

## 주의 (트레이드오프)
- 중복 허용은 결합도↓이나, 한쪽 수정 시 다른 쪽 누락으로 [[응집도|응집도]]↓ 위험. 상황 판단.
- Context는 진정한 전역 관심사만 (테마·로케일·인증·10개 이상 컴포넌트가 쓰는 데이터). 조합 패턴 피하려고 남용 금지.

## 관련 문서
[[코드 품질]] · [[응집도]] · [[FSD 폴더구조]]

## 출처
- 토스 FF (`10_raw/toss/frontend-fundamentals/code-quality/code/index.md` + `examples/`)
- 스킬 파일 (`10_raw/toss/frontend-fundamentals-skills/coupling.md`)
