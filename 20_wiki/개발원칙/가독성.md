---
type: concept
platform: common
status: verified
tags: [frontend, code-quality, readability]
source: 10_raw/toss/frontend-fundamentals/code-quality/
created: 2026-06-19
updated: 2026-06-19
---

# 가독성

## 한 줄 요약
코드가 읽기 쉬운 정도. 읽는 사람이 **한 번에 고려할 맥락이 적고**, 위→아래로 자연스럽게 읽히는 코드. [[코드 품질]] 4기준 중 하나.

## 전략
### 맥락 줄이기
- **같이 실행되지 않는 코드 분리** — 한 컴포넌트/함수에 여러 분기 책임 섞지 않기. (raw: `examples/submit-button.md`)
- **구현 상세 추상화** — 고수준 흐름과 저수준 디테일 분리. (raw: `examples/login-start-page.md`)
- **로직 종류로 함수 쪼개기** — 한 함수에 성격 다른 로직 합치지 않기. (raw: `examples/use-page-state-readability.md`)

### 이름 붙이기
- **복잡한 조건에 이름** — `if (a && b || c)` → 의미 있는 변수로. (raw: `examples/condition-name.md`)
- **매직 넘버에 이름** — `404`, `86400` 등 → 상수. (raw: `examples/magic-number-readability.md`)

### 위에서 아래로 읽히게
- **시점 이동 줄이기** — 코드 따라가며 위아래 왔다갔다 안 하게. (raw: `examples/user-policy.md`)
- **삼항 연산자 단순화** — 중첩 삼항 풀기. (raw: `examples/ternary-operator.md`)

## 빠른 참조

| 코드 냄새 | 개선 방법 |
|---|---|
| 중첩된 삼항 연산자 | `if`문이나 early return 사용 |
| 복잡한 조건 `a && !b \|\| c` | 의미있는 이름 붙이기: `const canProceed = ...` |
| 여러 분기가 교차됨 | 분기별로 별도 컴포넌트 분리 |
| 코드 위아래로 왔다갔다 | 조건을 한눈에 볼 수 있는 객체로 관리 |

## 코드 예제

### 동시에 실행되지 않는 코드 분리

❌ viewer/admin 로직이 한 컴포넌트에 섞임:
```tsx
function SubmitButton() {
  const isViewer = useRole() === "viewer";
  useEffect(() => {
    if (isViewer) return;
    showButtonAnimation();
  }, [isViewer]);
  return isViewer ? (
    <TextButton disabled>Submit</TextButton>
  ) : (
    <Button type="submit">Submit</Button>
  );
}
```

✅ 분기별로 분리:
```tsx
function SubmitButton() {
  const isViewer = useRole() === "viewer";
  return isViewer ? <ViewerSubmitButton /> : <AdminSubmitButton />;
}
function ViewerSubmitButton() {
  return <TextButton disabled>Submit</TextButton>;
}
function AdminSubmitButton() {
  useEffect(() => { showButtonAnimation(); }, []);
  return <Button type="submit">Submit</Button>;
}
```

### 복잡한 조건에 이름 붙이기

❌ 조건이 무슨 뜻인지 바로 안 보임:
```tsx
if (user.age >= 18 && !user.isBanned && user.emailVerified) { ... }
```

✅ 의도를 이름으로 표현:
```tsx
const canPurchase = user.age >= 18 && !user.isBanned && user.emailVerified;
if (canPurchase) { ... }
```

## 주의 (트레이드오프)
- 응집도를 위한 추상화는 가독성을 떨어뜨릴 수 있음 → [[응집도]] 참고. 위험 낮으면 가독성 우선.
- 간단한 로직까지 과도하게 추상화하지 말 것.
- 나쁜 코드에 주석 대신 코드 자체를 개선.

## 관련 문서
[[코드 품질]] · [[예측 가능성]] · [[응집도]] · [[결합도]]

## 출처
- 토스 FF (`10_raw/toss/frontend-fundamentals/code-quality/code/index.md` + `examples/`)
- 스킬 파일 (`10_raw/toss/frontend-fundamentals-skills/readability.md`)
