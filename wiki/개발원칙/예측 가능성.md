---
type: concept
platform: common
status: verified
tags: [frontend, code-quality, predictability]
source: raw/toss/frontend-fundamentals/code-quality/
created: 2026-06-19
updated: 2026-06-19
---

# 예측 가능성

## 한 줄 요약
동료가 함수·컴포넌트의 동작을 얼마나 예측할 수 있는지. **이름·파라미터·반환값만 보고** 동작을 알 수 있고, 일관된 규칙을 따르는 코드. [[코드 품질]] 4기준 중 하나.

## 전략
- **이름 겹치지 않게 관리** — 같은 이름이 다른 동작을 하면 예측 깨짐 (예: 커스텀 래퍼와 원본 라이브러리 함수 동명). (raw: `examples/http.md`)
- **같은 종류 함수는 반환 타입 통일** — 유사 함수가 어떤 건 값, 어떤 건 Promise 반환하면 혼란. (raw: `examples/use-user.md`)
- **숨은 로직 드러내기** — 이름·시그니처에 없는 부수효과(side effect)를 함수 안에 숨기지 않기. (raw: `examples/hidden-logic.md`)

## 왜 중요한가
- 예측 불가능한 코드는 호출부에서 매번 구현을 열어봐야 함 → [[가독성]]·생산성 저하.
- 숨은 부수효과는 버그·[[결합도|결합도]] 증가의 원천.

## 빠른 참조

| 코드 냄새 | 개선 방법 |
|---|---|
| `getX()`에 부수 효과 | 순수 getter와 action 분리 |
| `calculateX()`가 저장함 | `calculateAndSaveX()`로 이름 변경 또는 분리 |
| 같은 종류인데 반환 타입 다름 | Discriminated Union: `{ ok: true } \| { ok: false; reason }` |
| 라이브러리 이름과 겹침 | 구분되는 이름 사용: `http` → `httpService` |

## 코드 예제

### 숨은 로직 드러내기

❌ fetch 함수에 숨은 부수 효과:
```tsx
async function fetchBalance(): Promise<number> {
  const balance = await http.get<number>("...");
  logging.log("balance_fetched"); // 숨은 부수 효과!
  return balance;
}
```

✅ 부수 효과는 호출부에서 명시적으로:
```tsx
async function fetchBalance(): Promise<number> {
  return await http.get<number>("...");
}

const balance = await fetchBalance();
logging.log("balance_fetched");
```

### 반환 타입 통일

❌ 같은 종류인데 반환 타입 다름:
```tsx
function checkIsNameValid(name: string) {
  return name.length > 0; // boolean
}
function checkIsAgeValid(age: number) {
  if (age < 18) return { ok: false, reason: "18세 이상" };
  return { ok: true }; // 객체
}
```

✅ Discriminated Union으로 통일:
```tsx
type ValidationResult = { ok: true } | { ok: false; reason: string };

function checkIsNameValid(name: string): ValidationResult {
  if (name.length === 0) return { ok: false, reason: "이름 필수" };
  return { ok: true };
}
function checkIsAgeValid(age: number): ValidationResult {
  if (age < 18) return { ok: false, reason: "18세 이상" };
  return { ok: true };
}
```

## 주의 (트레이드오프)
- 부수 효과를 제거하지 말고 명시적 위치로 이동.
- 지나치게 복잡한 타입 피하기.
- 기존 API 계약 유지.

## 관련 문서
[[코드 품질]] · [[가독성]] · [[응집도]] · [[결합도]]

## 출처
- 토스 FF (`raw/toss/frontend-fundamentals/code-quality/code/index.md` + `examples/`)
- 스킬 파일 (`raw/toss/frontend-fundamentals-skills/predictability.md`)
